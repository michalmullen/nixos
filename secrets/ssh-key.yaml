# PLACEHOLDER — this file must be replaced with a sops-encrypted version
# before running nixos-rebuild switch on any machine.
#
# Steps:
#   1. Complete bootstrap (generate machine GPG keys, update .sops.yaml)
#   2. Copy your real SSH private key into this file under ssh_private_key
#   3. Run:  sops -e -i secrets/ssh-key.yaml
#   4. Commit the encrypted file
#
# The expected plaintext structure (before encryption) is:
#
#   ssh_private_key: |
#     -----BEGIN OPENSSH PRIVATE KEY-----
#     <your key here>
#     -----END OPENSSH PRIVATE KEY-----

ssh_private_key: "PLACEHOLDER - run setup commands then encrypt this file with sops"
